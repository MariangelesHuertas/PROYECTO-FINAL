import { combineReducers } from 'redux';

const rootReducer = combineReducers({
  // AQUI DEBERIAMOS DEFINIR LOS REDUCERS , INICLAMENTE DARA ERROR POR QUE ESTO ESTA VACIO
});

export default rootReducer;